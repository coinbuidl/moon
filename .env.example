# Required runtime path (must set)
OPENCLAW_BIN=/absolute/path/to/openclaw

# Runtime paths
MOON_HOME=$HOME/MOON
MOON_ARCHIVES_DIR=$MOON_HOME/archives
MOON_MEMORY_DIR=$MOON_HOME/memory
MOON_MEMORY_FILE=$MOON_HOME/MEMORY.md
MOON_LOGS_DIR=$MOON_HOME/MOON/logs
MOON_CONFIG_PATH=$MOON_HOME/MOON/moon.toml
MOON_STATE_FILE=$MOON_HOME/state/moon_state.json
# Optional alternative to MOON_STATE_FILE:
# MOON_STATE_DIR=$MOON_HOME/state
OPENCLAW_SESSIONS_DIR=$HOME/.openclaw/agents/main/sessions
QMD_BIN=$HOME/.bun/bin/qmd
QMD_DB=$HOME/.cache/qmd/index.sqlite

# Keep tuning in moon.toml (copy moon.toml.example), not in .env.
# Tuning sections:
# - [thresholds]
# - [watcher]
# - [distill]
# - [retention]
# - [inbound_watch]

# Distill provider profiles (choose ONE; leave others commented)
#
# OpenAI profile:
# MOON_DISTILL_PROVIDER=openai
# MOON_DISTILL_MODEL=gpt-4.1-mini
# OPENAI_API_KEY=sk-...
#
# Gemini profile:
# MOON_DISTILL_PROVIDER=gemini
# MOON_DISTILL_MODEL=gemini-2.5-flash-lite
# GEMINI_API_KEY=...
#
# Anthropic profile:
# MOON_DISTILL_PROVIDER=anthropic
# MOON_DISTILL_MODEL=claude-3-5-haiku-latest
# ANTHROPIC_API_KEY=...
#
# DeepSeek profile (OpenAI-compatible):
# MOON_DISTILL_PROVIDER=openai-compatible
# MOON_DISTILL_MODEL=deepseek-chat
# AI_BASE_URL=https://api.deepseek.com
# AI_API_KEY=...
#
# Local-only profile (no remote API key):
# MOON_DISTILL_PROVIDER=local

# Env-only distill/runtime controls
# - numeric (e.g. 524288)
# - auto (infer from distill model context limit when possible)
MOON_DISTILL_CHUNK_BYTES=auto
MOON_DISTILL_MAX_CHUNKS=128
# Optional explicit token context hint for auto mode.
# MOON_DISTILL_MODEL_CONTEXT_TOKENS=250000

# Set to 0 to disable high-token background alerts.
MOON_HIGH_TOKEN_ALERT_THRESHOLD=1000000

# Safety flags (explicitly opt in)
MOON_ENABLE_COMPACTION_WRITE=true
MOON_ENABLE_SESSION_ROLLOVER=true

# Optional advanced aliases (usually not needed):
# AI_MODEL=
# AI_PROVIDER=
# DEEPSEEK_API_KEY=
# MOON_GEMINI_MODEL=gemini-2.5-flash-lite
